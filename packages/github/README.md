# flybridge-github

Optional GitHub Project listing, authored pull-request search, issue-development facts, and pull-request fact adapter for Flybridge. Commands authenticate as `github.login` via a per-process `GH_TOKEN`. Pull-request facts may include author login, GitHub Actor `is_bot`, and optional review/comment/thread bodies.
