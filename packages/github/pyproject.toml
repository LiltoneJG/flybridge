[project]
name = "flybridge-github"
version = "1.0.0"
description = "Optional GitHub Project adapter for Flybridge"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
dependencies = ["flybridge-core"]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
